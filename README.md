# KATA-007A: SalesReporter

> Consulte `quests/dojo/java-21-certified/catalog.yml` no repo `skillforge` para a spec completa.

## Como resolver

1. Abra `src/main/java/com/skillforge/kata/SalesReporter.java`
2. Leia os testes em `src/test/java/com/skillforge/kata/SalesReporterTest.java`
3. Implemente até todos os testes passarem
4. Verifique: `mvn verify`

## Como submeter

```bash
git checkout -b kata-007a-{heroId}-solution
git commit -am "kata-007a: implement SalesReporter"
git push origin kata-007a-{heroId}-solution
# Abra PR com body: heroId: {heroId}
```
