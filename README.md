# KATA-008B: TriFunction

> Consulte `quests/dojo/java-21-certified/catalog.yml` no repo `skillforge` para a spec completa.

## Como resolver

1. Abra `src/main/java/com/skillforge/kata/TriFunction.java`
2. Leia os testes em `src/test/java/com/skillforge/kata/TriFunctionTest.java`
3. Implemente até todos os testes passarem
4. Verifique: `mvn verify`

## Como submeter

```bash
git checkout -b kata-008b-{heroId}-solution
git commit -am "kata-008b: implement TriFunction"
git push origin kata-008b-{heroId}-solution
# Abra PR com body: heroId: {heroId}
```
