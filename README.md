# KATA-007C: RunningAverageCollector

> Consulte `quests/dojo/java-21-certified/catalog.yml` no repo `skillforge` para a spec completa.

## Como resolver

1. Abra `src/main/java/com/skillforge/kata/RunningAverageCollector.java`
2. Leia os testes em `src/test/java/com/skillforge/kata/RunningAverageCollectorTest.java`
3. Implemente até todos os testes passarem
4. Verifique: `mvn verify`

## Como submeter

```bash
git checkout -b kata-007c-{heroId}-solution
git commit -am "kata-007c: implement RunningAverageCollector"
git push origin kata-007c-{heroId}-solution
# Abra PR com body: heroId: {heroId}
```
