# KATA-012A: ModuleConsumer

> Consulte `quests/dojo/java-21-certified/catalog.yml` no repo `skillforge` para a spec completa.

## Como resolver

1. Abra `src/main/java/com/skillforge/kata/ModuleConsumer.java`
2. Leia os testes em `src/test/java/com/skillforge/kata/ModuleConsumerTest.java`
3. Implemente até todos os testes passarem
4. Verifique: `mvn verify`

## Como submeter

```bash
git checkout -b kata-012a-{heroId}-solution
git commit -am "kata-012a: implement ModuleConsumer"
git push origin kata-012a-{heroId}-solution
# Abra PR com body: heroId: {heroId}
```
