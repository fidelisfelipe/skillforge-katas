# KATA-010B: MultiIndexMap

> Consulte `quests/dojo/java-21-certified/catalog.yml` no repo `skillforge` para a spec completa.

## Como resolver

1. Abra `src/main/java/com/skillforge/kata/MultiIndexMap.java`
2. Leia os testes em `src/test/java/com/skillforge/kata/MultiIndexMapTest.java`
3. Implemente até todos os testes passarem
4. Verifique: `mvn verify`

## Como submeter

```bash
git checkout -b kata-010b-{heroId}-solution
git commit -am "kata-010b: implement MultiIndexMap"
git push origin kata-010b-{heroId}-solution
# Abra PR com body: heroId: {heroId}
```
