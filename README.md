# KATA-015C: ScopeRules

> Consulte `quests/dojo/java-21-certified/catalog.yml` no repo `skillforge` para a spec completa.

## Como resolver

1. Abra `src/main/java/com/skillforge/kata/ScopeRules.java`
2. Leia os testes em `src/test/java/com/skillforge/kata/ScopeRulesTest.java`
3. Implemente até todos os testes passarem
4. Verifique: `mvn verify`

## Como submeter

```bash
git checkout -b kata-015c-{heroId}-solution
git commit -am "kata-015c: implement ScopeRules"
git push origin kata-015c-{heroId}-solution
# Abra PR com body: heroId: {heroId}
```
