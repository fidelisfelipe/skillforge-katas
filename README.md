# KATA-003C: EventHandler

> Consulte `quests/dojo/java-21-certified/catalog.yml` no repo `skillforge` para a spec completa.

## Como resolver

1. Abra `src/main/java/com/skillforge/kata/EventHandler.java`
2. Leia os testes em `src/test/java/com/skillforge/kata/EventHandlerTest.java`
3. Implemente até todos os testes passarem
4. Verifique: `mvn verify`

## Como submeter

```bash
git checkout -b kata-003c-{heroId}-solution
git commit -am "kata-003c: implement EventHandler"
git push origin kata-003c-{heroId}-solution
# Abra PR com body: heroId: {heroId}
```
