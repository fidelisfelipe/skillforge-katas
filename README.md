# FHIR-022: FhirSearchUrlBuilder

> Consulte `quests/dojo/java-21-certified/catalog.yml` no repo `skillforge` para a spec completa.

## Como resolver

1. Abra `src/main/java/com/skillforge/kata/FhirSearchUrlBuilder.java`
2. Leia os testes em `src/test/java/com/skillforge/kata/FhirSearchUrlBuilderTest.java`
3. Implemente até todos os testes passarem
4. Verifique: `mvn verify`

## Como submeter

```bash
git checkout -b fhir-022-{heroId}-solution
git commit -am "fhir-022: implement FhirSearchUrlBuilder"
git push origin fhir-022-{heroId}-solution
# Abra PR com body: heroId: {heroId}
```
