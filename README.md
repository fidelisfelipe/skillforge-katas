# KATA-016D: LogicalOps

> Consulte `quests/dojo/java-21-certified/catalog.yml` no repo `skillforge` para a spec completa.

## Como resolver

1. Abra `src/main/java/com/skillforge/kata/LogicalOps.java`
2. Leia os testes em `src/test/java/com/skillforge/kata/LogicalOpsTest.java`
3. Implemente até todos os testes passarem
4. Verifique: `mvn verify`

## Como submeter

```bash
git checkout -b kata-016d-{heroId}-solution
git commit -am "kata-016d: implement LogicalOps"
git push origin kata-016d-{heroId}-solution
# Abra PR com body: heroId: {heroId}
```
